import{createFileRoute,Link,useNavigate}from'@tanstack/react-router';import{useEffect,useState}from'react';import{supabase}from'../integrations/supabase/client';import{addEventTask,completeEventTask,loadCommercialData,setEventStatus,shortDate}from'../lib/erp-data';import{canAccess,canWrite,roleLabel}from'../lib/permissions';export const Route=createFileRoute('/operacoes')({component:Operations});type Row=Record<string,any>;type Ctx={company_id:string;company_name:string;access_allowed:boolean;role:string};function Operations(){const nav=useNavigate(),[ctx,setCtx]=useState<Ctx|null>(null),[d,setD]=useState<any>({events:[],tasks:[],reservations:[],inventory:[]}),[error,setError]=useState(''),[selected,setSelected]=useState(''),[busy,setBusy]=useState('');async function refresh(){try{setD(await loadCommercialData())}catch(e:any){setError(e.message)}}useEffect(()=>{void(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){nav({to:'/entrar'});return}const r=await supabase.rpc('my_company_context');if(r.error){setError(r.error.message);return}const x=(Array.isArray(r.data)?r.data[0]:r.data)as Ctx;setCtx(x);if(x?.access_allowed&&canAccess(x.role,'operacao'))await refresh()})()},[]);async function status(e:Row,next:string){if(busy)return;setBusy(e.id);setError('');try{await setEventStatus(e.id,next);await refresh()}catch(x:any){setError(x.message)}finally{setBusy('')}}if(!ctx)return <Gate text={error||'Carregando operação...'}/>;if(!canAccess(ctx.role,'operacao'))return <Denied ctx={ctx}/>;const write=canWrite(ctx.role,'operacao'),active=d.events.filter((e:Row)=>!['finalizado','cancelado'].includes(e.status));return <div className="heaven-shell"><aside className="heaven-sidebar"><img src="/heaven-logo.svg" className="sidebar-logo" alt="Heaven ERP"/><nav className="heaven-nav"><Link to="/app">Visão geral</Link><b>Produção e Logística</b>{canAccess(ctx.role,'financeiro')&&<Link to="/financeiro">Financeiro</Link>}{canAccess(ctx.role,'fiscal')&&<Link to="/fiscal">Notas Fiscais</Link>}{canAccess(ctx.role,'configuracoes')&&<Link to="/configuracoes">Configurações</Link>}</nav><div className="heaven-company"><b>{ctx.company_name}</b><br/>{roleLabel(ctx.role)}</div></aside><main className="heaven-main"><header className="heaven-header"><div><span className="heaven-eyebrow">{ctx.company_name}</span><h1 className="heaven-title">Central de Produção</h1><span className="heaven-muted">Separação, entrega, montagem, retirada, devolução e conferência por evento.</span></div></header>{!write&&<div className="heaven-alert"><span>Modo consulta: seu perfil pode acompanhar a operação, mas não alterar registros.</span></div>}{error&&<div className="heaven-alert">{error}<button onClick={()=>setError('')}>×</button></div>}<div className="heaven-grid heaven-kpis"><K n={active.length} t="Eventos ativos"/><K n={d.tasks.filter((x:Row)=>!x.completed_at).length} t="Tarefas pendentes"/><K n={d.reservations.length} t="Itens reservados"/><K n={active.filter((x:Row)=>x.status==='aguardando_devolucao').length} t="Aguardando devolução"/></div>{active.map((e:Row)=>{const tasks=d.tasks.filter((t:Row)=>t.event_id===e.id),reservations=d.reservations.filter((r:Row)=>r.event_id===e.id),pending=tasks.filter((t:Row)=>!t.completed_at).length,returning=e.status==='aguardando_devolucao';return <section className="heaven-card module-card" key={e.id}><div className="card-head"><div><h2>{e.title}</h2><span className="heaven-muted">{shortDate(e.event_date)} · {e.city||'local a definir'} · {String(e.status).replaceAll('_',' ')}</span></div>{write&&<div className="workspace-actions"><button disabled={!!busy} onClick={()=>setSelected(selected===e.id?'':e.id)}>+ Tarefa</button>{!returning&&<button disabled={!!busy} onClick={()=>status(e,'aguardando_devolucao')}>Registrar retirada / aguardar devolução</button>}<button className="primary" disabled={!!busy} title={pending?'Conclua o checklist antes de finalizar':''} onClick={()=>status(e,'finalizado')}>{busy===e.id?'Salvando...':returning?'Conferir e finalizar':'Finalizar evento'}</button></div>}</div>{returning&&<div className="heaven-alert"><span><b>Devolução pendente.</b> Confira peças, quantidades, avarias e itens faltantes no checklist antes de finalizar.</span></div>}{pending>0&&<div className="heaven-muted">{pending} tarefa(s) pendente(s). A finalização é bloqueada até o checklist estar concluído.</div>}{selected===e.id&&write&&<form className="trial-form" onSubmit={async ev=>{ev.preventDefault();const f=Object.fromEntries(new FormData(ev.currentTarget).entries())as Record<string,string>;try{await addEventTask(ctx.company_id,e.id,f);await refresh();setSelected('')}catch(x:any){setError(x.message)}}}><div className="form-grid"><label>Etapa<select name="category"><option value="producao">Produção</option><option value="separacao">Separação</option><option value="entrega">Entrega</option><option value="montagem">Montagem</option><option value="retirada">Retirada</option><option value="devolucao">Devolução</option><option value="conferencia">Conferência</option><option value="avaria">Avaria</option><option value="item_faltante">Item faltante</option></select></label><label>Prazo<input type="datetime-local" name="dueAt"/></label></div><label>Tarefa<input required name="title" placeholder="Ex.: conferir painel romano na devolução"/></label><label>Observação<input name="notes" placeholder="Detalhes, avaria, quantidade faltante..."/></label><button className="primary">Adicionar ao checklist</button></form>}<h3>Checklist operacional</h3>{tasks.length?tasks.map((t:Row)=><div className="heaven-task" key={t.id}><div><b>{t.title}</b><small>{String(t.category).replaceAll('_',' ')} · {t.due_at?shortDate(t.due_at):'sem prazo'}{t.notes?` · ${t.notes}`:''}</small></div>{write&&<button disabled={!!busy} onClick={async()=>{try{await completeEventTask(t.id,!t.completed_at);await refresh()}catch(x:any){setError(x.message)}}}>{t.completed_at?'Reabrir':'Concluir'}</button>}</div>):<Empty text="Nenhuma tarefa operacional criada."/>}<h3>Acervo reservado</h3>{reservations.length?reservations.map((r:Row)=>{const item=d.inventory.find((i:Row)=>i.id===r.item_id);return <div className="heaven-task" key={r.id}><div><b>{item?.name||'Item de estoque'}</b><small>{r.quantity} un. · {shortDate(r.reserved_from)} a {shortDate(r.reserved_until)}</small></div></div>}):<Empty text="Ainda não há itens reservados para este evento."/>}</section>})}{!active.length&&<Empty text="Nenhum evento ativo aguardando operação."/>}</main></div>}function K({n,t}:any){return <div className="heaven-card"><small>{t}</small><div className="heaven-value">{n}</div></div>}function Empty({text}:any){return <div className="empty-state"><b>Tudo certo por aqui.</b><span>{text}</span></div>}function Denied({ctx}:{ctx:Ctx}){return <div className="app-gate"><div><img src="/heaven-logo.svg" alt="Heaven ERP" style={{width:220}}/><h1>Acesso restrito</h1><p>O perfil <b>{roleLabel(ctx.role)}</b> não possui acesso à Produção e Logística.</p><Link to="/app" className="primary-link">Voltar</Link></div></div>}function Gate({text}:{text:string}){return <div className="app-gate"><div><img src="/heaven-logo.svg" alt="Heaven ERP" style={{width:220}}/><p>{text}</p><Link to="/entrar" className="primary-link">Ir para acesso</Link></div></div>}
