import{createFileRoute,Link}from'@tanstack/react-router';import{useState}from'react';import{supabase}from'../integrations/supabase/client';export const Route=createFileRoute('/recuperar-senha')({component:Recover});function Recover(){const[sent,setSent]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const email=String(new FormData(e.currentTarget).get('email')||'').trim().toLowerCase(),redirectTo=`${window.location.origin}/nova-senha`;const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo});if(error){setError(error.message);return}setSent(true)}return <div className="app-gate"><div><div className="brand">heaven <small>PLATAFORMA</small></div><h1>Recuperar senha</h1>{sent?<><p>Se o e-mail estiver cadastrado, enviaremos o link seguro para definir uma nova senha.</p><Link to="/entrar" className="primary-link">Voltar ao acesso</Link></>:<form className="trial-form" onSubmit={submit}><label>E-mail<input required type="email" name="email" autoComplete="email"/></label>{error&&<div className="form-error">{error}</div>}<button className="primary">Enviar link de recuperação</button><Link to="/entrar">Voltar</Link></form>}</div></div>}
