import { createFileRoute,Link,useNavigate } from '@tanstack/react-router'
import { useEffect,useMemo,useState } from 'react'
import { supabase } from '../integrations/supabase/client'
import { activateContract,addQuoteItem,convertLeadToClient,createContract,createInventoryItem,createLead,createQuoteFromLead,loadCommercialData,money,removeQuoteItem,reserveInventory,setQuoteStatus,shortDate } from '../lib/erp-data'
export const Route=createFileRoute('/app')({component:Workspace})
type Ctx={company_id:string;company_name:string;company_status:string;role:string;trial_ends_at:string|null;days_remaining:number|null;access_allowed:boolean};type Row=Record<string,any>
const local=['Hoje','CRM e Leads','Clientes','Orçamentos','Contratos','Agenda e Eventos','Estoque'];
function Workspace(){const nav=useNavigate();const[ctx,setCtx]=useState<Ctx|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[active,setActive]=useState('Hoje'),[data,setData]=useState<any>({leads:[],clients:[],quotes:[],quoteItems:[],contracts:[],events:[],inventory:[],reservations:[]}),[busy,setBusy]=useState(''),[modal,setModal]=useState(''),[selected,setSelected]=useState<Row|null>(null);useEffect(()=>{void boot()},[]);async function boot(){const{data:{user}}=await supabase.auth.getUser();if(!user){nav({to:'/entrar'});return}await supabase.rpc('activate_my_pending_trial');const r=await supabase.rpc('my_company_context');if(r.error){setError(r.error.message);setLoading(false);return}const row=(Array.isArray(r.data)?r.data[0]:r.data)as Ctx;setCtx(row);if(row?.access_allowed)await refresh();setLoading(false)}async function refresh(){try{setData(await loadCommercialData())}catch(e:any){setError(e.message)}}async function run(id:string,fn:()=>Promise<any>,next?:string){setBusy(id);try{await fn();await refresh();if(next)setActive(next)}catch(e:any){setError(e.message)}finally{setBusy('')}}async function logout(){await supabase.auth.signOut();nav({to:'/entrar'})}const openQuotes=data.quotes.filter((q:Row)=>!['recusado','cancelado','expirado'].includes(q.status)),proposal=useMemo(()=>openQuotes.reduce((s:number,q:Row)=>s+Number(q.total||0),0),[data.quotes]);if(loading)return <Gate text="Carregando seu ambiente Heaven..."/>;if(!ctx)return <Gate text={error||'Ambiente não encontrado.'}/>;if(!ctx.access_allowed)return <Gate text="Seu teste terminou. O ambiente e todos os dados continuam preservados."/>;return <div className="heaven-shell"><aside className="heaven-sidebar"><div className="heaven-brand">heaven <small>PLATAFORMA</small></div><nav className="heaven-nav">{local.map(x=><button key={x} className={active===x?'active':''} onClick={()=>setActive(x)}>{x}</button>)}<Link to="/operacoes">Produção e Logística</Link><Link to="/financeiro">Financeiro e Gestão</Link><Link to="/configuracoes">Configurações</Link></nav><div className="heaven-company"><b>{ctx.company_name}</b><br/>{ctx.role}</div></aside><main className="heaven-main"><header className="heaven-header"><div><span className="heaven-eyebrow">{ctx.company_name}</span><h1 className="heaven-title">{active}</h1><span className="heaven-muted">Da reserva à devolução, sem redigitação.</span></div><div className="workspace-actions">{ctx.days_remaining!=null&&<span className="trial-counter">Teste · {ctx.days_remaining} dias</span>}<button className="secondary-link" onClick={logout}>Sair</button></div></header>{error&&<div className="heaven-alert"><b>Atenção</b><span>{error}</span><button onClick={()=>setError('')}>×</button></div>}{active==='Hoje'&&<Today d={data} proposal={proposal} go={setActive} add={()=>setModal('lead')}/>} {active==='CRM e Leads'&&<Leads rows={data.leads} busy={busy} add={()=>setModal('lead')} quote={(id:string)=>run(id,()=>createQuoteFromLead(id),'Orçamentos')} client={(id:string)=>run(id,()=>convertLeadToClient(id),'Clientes')}/>} {active==='Clientes'&&<Simple rows={data.clients}/>} {active==='Orçamentos'&&<Quotes d={data} busy={busy} open={(q:Row)=>{setSelected(q);setModal('quote')}} contract={(q:Row)=>run(q.id,async()=>{if(!q.client_id&&q.lead_id)await convertLeadToClient(q.lead_id);await setQuoteStatus(q.id,'aprovado');await createContract(q.id)},'Contratos')}/>} {active==='Contratos'&&<Contracts rows={data.contracts} busy={busy} activate={(c:Row)=>run(c.id,()=>activateContract(c.id),'Agenda e Eventos')}/>} {active==='Agenda e Eventos'&&<Events rows={data.events} reserve={(e:Row)=>{setSelected(e);setModal('reserve')}}/>} {active==='Estoque'&&<Inventory rows={data.inventory} add={()=>setModal('inventory')}/>}</main><nav className="heaven-mobile-nav">{local.slice(0,5).map(x=><button key={x} className={active===x?'active':''} onClick={()=>setActive(x)}>{x}</button>)}</nav>{modal==='lead'&&<Modal title="Novo lead" close={()=>setModal('')}><form className="trial-form" onSubmit={async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget).entries())as Record<string,string>;await run('lead',()=>createLead(ctx.company_id,f));setModal('')}}><L n="Nome" k="name" req/><div className="form-grid"><L n="WhatsApp" k="phone" req/><L n="E-mail" k="email"/></div><div className="form-grid"><L n="Tipo do evento" k="eventType"/><label>Data<input name="eventDate" type="date"/></label></div><L n="Cidade" k="city"/><L n="Observações" k="notes"/><button className="primary">Salvar lead</button></form></Modal>}{modal==='quote'&&selected&&<QuoteEditor q={selected} items={data.quoteItems.filter((x:Row)=>x.quote_id===selected.id)} close={()=>setModal('')} add={(f:any)=>run('item',()=>addQuoteItem(ctx.company_id,selected.id,f))} remove={(id:string)=>run(id,()=>removeQuoteItem(id,selected.id))}/>} {modal==='inventory'&&<Modal title="Novo item de estoque" close={()=>setModal('')}><form className="trial-form" onSubmit={async e=>{e.preventDefault();await run('inv',()=>createInventoryItem(ctx.company_id,Object.fromEntries(new FormData(e.currentTarget).entries())as Record<string,string>));setModal('')}}><L n="Nome" k="name" req/><L n="Quantidade" k="quantity" req/><L n="Custo de reposição" k="cost"/><button className="primary">Cadastrar item</button></form></Modal>}{modal==='reserve'&&selected&&<Modal title="Reservar item para o evento" close={()=>setModal('')}><form className="trial-form" onSubmit={async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget).entries())as Record<string,string>,start=new Date(selected.starts_at),end=selected.ends_at?new Date(selected.ends_at):new Date(start.getTime()+86400000);await run('reserve',()=>reserveInventory(selected.id,f.itemId,Number(f.quantity),start.toISOString(),end.toISOString()));setModal('')}}><label>Item<select required name="itemId">{data.inventory.map((i:Row)=><option value={i.id} key={i.id}>{i.name} · {i.total_quantity} un.</option>)}</select></label><L n="Quantidade" k="quantity" req/><button className="primary">Confirmar reserva</button></form></Modal>}</div>}
function Today({d,proposal,go,add}:any){const novos=d.leads.filter((x:Row)=>x.status==='novo');return <><div className="heaven-grid heaven-kpis"><K n={novos.length} t="Novos leads"/><K n={d.events.length} t="Eventos"/><K n={d.contracts.length} t="Contratos"/><K n={money(proposal)} t="Em propostas"/></div><div className="heaven-grid heaven-columns"><section className="heaven-card"><div className="card-head"><h2>Precisa da sua atenção</h2><button className="primary" onClick={add}>+ Novo lead</button></div>{novos.length?novos.slice(0,6).map((l:Row)=><div className="heaven-task" key={l.id}><div><b>{l.name}</b><small>{l.event_type||'Evento'} · {shortDate(l.event_date)}</small></div><button onClick={()=>go('CRM e Leads')}>Abrir</button></div>):<Empty/>}</section><section className="heaven-card"><h2>Fluxo conectado</h2><p className="heaven-muted">Lead → Orçamento → Contrato → Evento → Operação → Financeiro.</p><Link to="/operacoes" className="primary-link">Abrir central operacional</Link></section></div></>}
function Leads({rows,busy,add,quote,client}:any){return <Card title={`Leads · ${rows.length}`} action={<button className="primary" onClick={add}>+ Novo lead</button>}><Table>{rows.map((l:Row)=><tr key={l.id}><td><b>{l.name}</b><small>{l.phone}</small></td><td>{l.event_type||'—'}</td><td>{shortDate(l.event_date)}</td><td><span className="heaven-status">{l.status}</span></td><td><button disabled={busy===l.id} onClick={()=>quote(l.id)}>Orçamento</button> <button onClick={()=>client(l.id)}>Cliente</button></td></tr>)}</Table></Card>};function Simple({rows}:any){return <Card title={`Clientes · ${rows.length}`}><Table>{rows.map((r:Row)=><tr key={r.id}><td><b>{r.name}</b><small>{r.email||'Sem e-mail'}</small></td><td>{r.phone||'—'}</td><td>{r.city||'—'}</td></tr>)}</Table></Card>};function Quotes({d,busy,open,contract}:any){return <Card title={`Orçamentos · ${d.quotes.length}`}><Table>{d.quotes.map((q:Row)=><tr key={q.id}><td><b>{q.title}</b><small>{shortDate(q.event_date)}</small></td><td>{q.status}</td><td><b>{money(Number(q.total||0))}</b></td><td><button onClick={()=>open(q)}>Editar</button> <button disabled={busy===q.id} onClick={()=>contract(q)}>Aprovar + contrato</button></td></tr>)}</Table></Card>};function Contracts({rows,busy,activate}:any){return <Card title={`Contratos · ${rows.length}`}><Table>{rows.map((c:Row)=><tr key={c.id}><td><b>{c.customer_snapshot?.name||'Cliente'}</b></td><td>{c.status}</td><td>{money(Number(c.commercial_snapshot?.total||0))}</td><td><button disabled={busy===c.id||c.status==='ativo'} onClick={()=>activate(c)}>Ativar + criar evento</button></td></tr>)}</Table></Card>};function Events({rows,reserve}:any){return <Card title={`Agenda e Eventos · ${rows.length}`}><Table>{rows.map((e:Row)=><tr key={e.id}><td><b>{e.title}</b><small>{e.city||'Local a definir'}</small></td><td>{shortDate(e.starts_at)}</td><td>{e.status}</td><td><button onClick={()=>reserve(e)}>Reservar estoque</button></td></tr>)}</Table></Card>};function Inventory({rows,add}:any){return <Card title={`Estoque · ${rows.length}`} action={<button className="primary" onClick={add}>+ Novo item</button>}><Table>{rows.map((i:Row)=><tr key={i.id}><td><b>{i.name}</b></td><td>{i.total_quantity} un.</td><td>{i.replacement_cost?money(Number(i.replacement_cost)):'—'}</td></tr>)}</Table></Card>}
function QuoteEditor({q,items,close,add,remove}:any){return <Modal title={q.title} close={close}><h2>{money(items.reduce((s:number,x:Row)=>s+Number(x.total||0),0))}</h2><Table>{items.map((i:Row)=><tr key={i.id}><td>{i.description}</td><td>{i.quantity} × {money(Number(i.unit_price))}</td><td>{money(Number(i.total))}</td><td><button onClick={()=>remove(i.id)}>Excluir</button></td></tr>)}</Table><form className="trial-form" onSubmit={e=>{e.preventDefault();void add(Object.fromEntries(new FormData(e.currentTarget).entries()));e.currentTarget.reset()}}><L n="Descrição" k="description" req/><div className="form-grid"><L n="Quantidade" k="quantity"/><L n="Valor unitário" k="unitPrice" req/></div><input type="hidden" name="itemType" value="servico"/><button className="primary">Adicionar item</button></form></Modal>};function Card({title,action,children}:any){return <section className="heaven-card module-card"><div className="card-head"><h2>{title}</h2>{action}</div>{children}</section>}function Table({children}:any){return !children?.length?<Empty/>:<div className="table-scroll"><table className="heaven-table"><tbody>{children}</tbody></table></div>}function Empty(){return <div className="empty-state"><b>Nada por aqui ainda.</b><span>Os registros aparecerão conforme o fluxo avançar.</span></div>}function K({n,t}:any){return <div className="heaven-card"><small>{t}</small><div className="heaven-value">{n}</div></div>}function L({n,k,req=false}:any){return <label>{n}<input required={req} name={k}/></label>}function Modal({title,close,children}:any){return <div className="modal-backdrop" onMouseDown={close}><div className="modal-card" onMouseDown={e=>e.stopPropagation()}><div className="card-head"><h2>{title}</h2><button onClick={close}>×</button></div>{children}</div></div>}function Gate({text}:{text:string}){return <div className="app-gate"><div><h1>Heaven Plataforma</h1><p>{text}</p><Link to="/entrar" className="primary-link">Ir para acesso</Link></div></div>}
